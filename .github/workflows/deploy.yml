name: Deploy ColonyGo

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy to VPS
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: 87.106.45.49
          username: root
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            /var/www/deploy.sh
